<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.BestPay.Dao.UserDao">
    <cache type="com.BestPay.Cache.RedisCache"/>
    <!-- 先定义一个Interface，UserMapper，然后Mapper文件的namespace指向的就是这个Interface， UserInterface里定义的方法和UserMapper.xml文件中的一样,
        然后代码里直接使用接口 -->

    <resultMap type="com.BestPay.entity.userEnity" id="UserResult">
        <result property="userID" column="id" />
        <result property="userName" column="userName" />
        <result property="password" column="password" />
    </resultMap>

    <!-- 所谓的动态SQL语句 -->


    <!-- 用户查询 -->
    <select id="find" parameterType="Map" resultMap="UserResult">
        select * from user
        <where>
            <if test="userName!=null and userName!=‘‘ ">
                and userName like #{userName}
            </if>
        </where>
        <if test="start!=null and size!=null">
            limit #{start},#{size}
        </if>
    </select>
    <select id="getAllUsers" resultMap="UserResult">
        SELECT  *FROM USER
    </select>
    <delete id="delete" parameterType="Integer">
        DELETE FROM USER WHERE id =#{userID}
    </delete>
</mapper>